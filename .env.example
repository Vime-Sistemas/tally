# API Configuration
# Leave empty for local development (uses Vite proxy to localhost:3000)
# Set to your deployed backend URL for production (e.g., https://your-ngrok-url.ngrok.io)
VITE_API_URL=

# Crypto Secret Key (use a strong random key in production)
VITE_CRYPTO_SECRET=your-secret-key-here
